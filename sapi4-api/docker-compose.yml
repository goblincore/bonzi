version: "3.8"

services:
  sapi4-tts:
    build: .
    ports:
      - "8085:8080"
    restart: unless-stopped
    # Optional: limit resources
    # deploy:
    #   resources:
    #     limits:
    #       memory: 512M
